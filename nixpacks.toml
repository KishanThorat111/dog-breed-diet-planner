# nixpacks.toml — let Nixpacks auto-detect Python and install requirements.txt.
# Do NOT override [phases.install] — pip is not in PATH at that stage.
# The start command is intentionally omitted here; it is already defined in
# both railway.json and Procfile, so Railway picks it up automatically.
