# Placeholder — place your fine-tuned dog breed ONNX model here.
# Expected filename: dog_breed_efficientnet.onnx
# Expected labels file: dog_breed_efficientnet_labels.txt  (one breed per line)
#
# To generate the model, run:
#   python scripts/train_breed_classifier.py
#
# Until the model is present, the app falls back to EfficientNetB3 (ImageNet weights).
